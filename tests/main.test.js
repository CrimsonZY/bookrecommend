/**
 * 测试入口占位：当前使用 Node 内置 test runner，无需额外依赖。
 * 若团队统一 Vitest/Jest，可迁移语法并配置 package.json。
 */

import test from "node:test";
import assert from "node:assert/strict";

test("占位：环境可用", () => {
  assert.ok(true);
});
